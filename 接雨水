题意：给一个数组，代表ai高度的宽度为1的柱子，相邻几个柱子围成的不封口的空间用来存放雨水，问能存放的雨水量是多少
解答：
最容易想到的就是直接扫描数组，然后对其进行统计，每个位置找到比他大的下一个柱子的位置然后进行雨水量统计，但总体的时间复杂度是O（n^2），空间复杂度是O（n）
我们要对它进行优化，我们想到的是用双指针进行优化
用到left和right两个指针，哪个小就先移动哪边进行操作，一个一个挪动，这样就优化到了O（n）的时间复杂度，和O（1）的空间复杂度
代码如下：
class Solution {
public:
    int trap(vector<int>& height) {
        int n=height.size();
        int ans=0;
        int left=0,right=n-1;
        int leftmax=0,rightmax=0;
        while(left<right){
            leftmax=max(height[left],leftmax);
            rightmax=max(height[right],rightmax);
            if(height[left]<height[right]){
                ans+=leftmax-height[left];
                ++left;
            }
            else{
                ans+=rightmax-height[right];
                --right;
            }
        }
        return ans;
    }
};
